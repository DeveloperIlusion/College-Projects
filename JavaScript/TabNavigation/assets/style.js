import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  viewTN: {
     flex: 1,
     alignItems: 'center',
     justifyContent: 'center',
  },
  textTN:{
    fontSize: 20,
    color: '#1c1c1c',
  }
})